<UserControl x:Class="DiiagramrPrimitives.AccumulatorNodeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:DiiagramrPrimitives"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             xmlns:s="https://github.com/canton7/Stylet">
    <TextBox
        Grid.Row="1"
        Grid.Column="0"
        Grid.ColumnSpan="2"
        HorizontalContentAlignment="Center"
        VerticalContentAlignment="Center"
        PreviewKeyDown="{s:Action PreviewKeyDown}"
        Text="{Binding StringValue, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
        FontSize="12" />
</UserControl>