<UserControl
    x:Class="Diiagramr.View.ShellScreen.ProjectScreen.ProjectScreenView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:projectScreen="clr-namespace:DiiagramrAPI.ViewModel.ProjectScreen;assembly=DiiagramrAPI"
    xmlns:s="https://github.com/canton7/Stylet"
    d:DataContext="{d:DesignInstance projectScreen:ProjectScreenViewModel}"
    d:DesignHeight="300"
    d:DesignWidth="300"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="175" />
            <ColumnDefinition Width="3" />
            <ColumnDefinition Width="40*" />
        </Grid.ColumnDefinitions>

        <ContentControl
            Grid.Column="0"
            Margin="2,2,0,1"
            s:View.Model="{Binding ProjectExplorerViewModel}" />

        <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" />

        <ContentControl
            Grid.Column="2"
            Margin="0,0,2,1"
            s:View.Model="{Binding DiagramWellViewModel}" />
    </Grid>
</UserControl>
