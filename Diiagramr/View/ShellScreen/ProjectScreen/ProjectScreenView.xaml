<UserControl x:Class="Diiagramr.View.ShellScreen.ProjectScreen.ProjectScreenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:s="https://github.com/canton7/Stylet"
             xmlns:viewModel="clr-namespace:DiiagramrAPI.ViewModel.ShellScreen.ProjectScreen;assembly=DiiagramrAPI"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance viewModel:ProjectScreenViewModel}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="175"/>
            <ColumnDefinition Width="3"/>
            <ColumnDefinition Width="40*"/>
        </Grid.ColumnDefinitions>
        
        <ContentControl Grid.Column="0" 
                        s:View.Model="{Binding ProjectExplorerViewModel}" 
                        Margin="2,2,0,1"></ContentControl>
        
        <GridSplitter Grid.Column="1" 
                      HorizontalAlignment="Stretch"/>
        
        <ContentControl Grid.Column="2" 
                        s:View.Model="{Binding DiagramWellViewModel}" 
                        Margin="0,0,2,1"/>
    </Grid>
</UserControl>
