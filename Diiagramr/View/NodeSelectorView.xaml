<UserControl x:Class="Diiagramr.View.NodeSelectorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:s="https://github.com/canton7/Stylet"
             xmlns:viewModel1="clr-namespace:Diiagramr.ViewModel"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance viewModel1:NodeSelectorViewModel}">
    <ItemsControl ItemsSource="{Binding AvailableNodeViewModels}" Width="200" HorizontalAlignment="Center" VerticalAlignment="Center">
        <ItemsControl.ItemContainerStyle>
            <Style TargetType="ContentPresenter">
                <Setter Property="ContentTemplate">
                    <Setter.Value>
                        <DataTemplate>
                            <Button Content="{Binding Name}" Command="{s:Action SelectNode}" CommandParameter="{Binding}"></Button>
                        </DataTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ItemsControl.ItemContainerStyle>
    </ItemsControl>
</UserControl>
